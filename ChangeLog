2005-11-20  FUNABARA Masao  <l07steps@users.sourceforge.jp>

	* trap.c : 関数の外部宣言のチェック

	* throw.c : 関数の外部宣言のチェック

	* spechit.c : 関数の外部宣言のチェック

	* score.c : 関数の外部宣言のチェック

	* save.c : 関数の外部宣言のチェック

	* room.c : 関数の外部宣言のチェック

	* ring.c : 関数の外部宣言のチェック

	* random.c : 関数の外部宣言のチェック

	* play.c : 関数の外部宣言のチェック

	* pack.c : 関数の外部宣言のチェック

	* object.c : 関数の外部宣言のチェック

	* move.c : 関数の外部宣言のチェック

	* monster.c : 関数の外部宣言のチェック

	* message.c : 関数の外部宣言のチェック

	* main.c : 関数の外部宣言のチェック

	* machdep.c : 関数の外部宣言のチェック

	* level.c : 関数の外部宣言のチェック

2005-11-18  FUNABARA Masao

	* invent.c : 関数の外部宣言のチェック

	* init.c : 関数の外部宣言のチェック

2005-11-17  FUNABARA Masao

	* hit.c : 関数の外部宣言のチェック

	* curese.c : 構造体名宣言のポインタ位置の修正

	* curses.c : 関数の外部宣言のチェック

	* curses.c : #ifdef を出来るだけ関数の範囲にまとめる

	* zap.c の Warning メッセージを出さないように修正

	* use.c の Warning メッセージを出さないように修正

	* throw.c の Warning メッセージを出さないように修正

	* spechit.c の Warning メッセージを出さないように修正

	* score.c の Warning メッセージを出さないように修正

	* save.c の Warning メッセージを出さないように修正

	* room.c の Warning メッセージを出さないように修正

	* ring.c の Warning メッセージを出さないように修正

	* pack.c の Warning メッセージを出さないように修正

	* move.c の Warning メッセージを出さないように修正

	* machdep.c の Warning メッセージを出さないように修正

	* invent.c の Warning メッセージを出さないように修正

	* curses.c の Warning メッセージを出さないように修正

	* trap.c の Warning メッセージを出さないように修正

	* play.c の Warning メッセージを出さないように修正

	* object.c の Warning メッセージを出さないように修正

	* monster.c の Warning メッセージを出さないように修正

	* hit.c の Warning メッセージを出さないように修正

	* message.c Warning メッセージを出さないように修正

	* level.c の Warning メッセージを出さないように修正

	* init.c の Warning メッセージを出ないように修正

	* init.c の Warning メッセージをほぼ出ないように修正

2005-11-16  FUNABARA Masao

	* main.c の Warning メッセージを出ないように修正

	* 発生していたコンパイルエラーを修正し、コンパイルを完了できるように修正する
	machdep.c : コンパイルエラーを修正

	* automake / autoconf 化
	AUTHORS, COPYING, ChangeLog, INSTALL, NEWS, README, TODO のメッセージ作成する
	Makefile.am, configure.ac のファイルを追加する

	* rogueclone2s のプログラム作成開始
	データ分離版ローグ・クローンIIのソースを CVS にそのまま入れる

